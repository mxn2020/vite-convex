/**
 * Placeholder API types for the shared Convex backend.
 *
 * Replace this file with the generated api.ts from the backend repo:
 *   cd the-mehdi-verse-convex-backend
 *   npx convex-helpers ts-api-spec
 *
 * Then copy the output here.
 */
import { anyApi } from "convex/server";

export const api = anyApi;
